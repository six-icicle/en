# Project Context — En

## What this is
**En** (縁 — connection, fate, the central point that links) is a desktop hub for managing many concurrent Claude CLI sessions on Mac. Tauri 2 app (Rust core + React/TypeScript webview frontend) with embedded terminals via xterm.js. Single hub window: themed grid of terminal sessions, every tile visible at once. *Not* a generic terminal app — purpose-built for running many `claude` instances side-by-side and knowing which need attention. Renamed from "multiclaude" 2026-05-20. Repo: `git@github.com:six-icicle/en.git`. Bundle ID: `dev.sixicicle.en`. Crate: `en` / `en_lib`.

## Why
User runs many `claude` sessions per day across different projects and wants one place to navigate, organize, and surface which sessions need input — instead of cycling through dozens of iTerm2 windows.

## Stack
- Frontend: React 19 + TypeScript + shadcn/ui + Vite + xterm.js (WebGL addon)
- Backend: Tauri 2 + Rust + portable-pty + tiny_http (status-detection HTTP receiver)
- Storage: SQLite via rusqlite (planned — metadata only; sessions are ephemeral, die on hub quit)
- Aesthetic: tea-ceremony minimalism, "lifted" lifted-bg recipe (the canonical en feel). **Brand wordmark: Outfit 600/16px**, body sans **Space Grotesk 500** (swapped from Inter), mono JetBrains Mono. **Default theme: rose-pine-soft + seigaiha texture**. **8 themes** (4 families × 2 variants) + 14 textures + accent/bg pickers + texture-amount and font-size scales. Full design law captured in `mockups/49-brand-blueprint.html` (visual + copy-pasteable JSON for AI hand-off).

## What's done
- Architecture: Tauri 2 + React/TS + portable-pty
- Layout: live tiling grid — every session visible at once, ⌘+arrows / click to focus, layout switcher (Row / Quad / Hex / Focus)
- Visual system: hyper-modern minimal, Inter + JetBrains Mono, **6 themes** (kanagawa, everforest, rose-pine, rose-pine-alt **default**, original / "Dark Amber", **original-alt / "Dark Amber (alt)"** — softer warm-charcoal w/ muted butterscotch accent)
- **Texture system: 14 options** in a 6-col popover grid. **12 normal** at 2-col span (off, grain, scanlines, dots, sakura, seigaiha, topography, pluses, yagasuri, brush, bokeh, bamboo) + **2 rain** at 3-col span on the bottom row (sakura rain, shuriken rain). All chips are uniform plain-tile + label, no per-chip preview swatches. Strength slider modulates `--tex-amt`.
- Active tile indicator: top accent line + bloom + 1px inset accent ring + soft inner glow
- Grid sizing rule: 1–5 sessions = single row full height; 6 sessions = 2×3; layouts cover counts 1–6 with no empty cells
- **Style C frosted-glass navbar** — translucent backdrop-blur pills with 1px hairline highlight on top edge, accent-glow ring on hover, scale-press on active. Layout: `[en wordmark + torii svg] [+] [↻] [app/term fz segmented controls] [theme pill] [layout pill] [alert pill]`. All buttons `flex: none + nowrap` so they never deform. Container query on `.titlebar`: at <860px, "THEME / LAYOUT / ALERT" labels and inner pill text collapse to icon-only.
- **Brand mark**: torii gate SVG before "en" wordmark, `currentColor` driven by `--accent`. Wordmark in Outfit 600/16px, scales with `--fz`.
- **Two font-size pills**: `app` scales chrome (`--fz`), `term` scales xterm canvas (`tfz`) independently. Both 0.7–1.6, persisted.
- **Accent fully wired through every surface**: spawn `+` icon, refresh ↻ icon, brand torii, static sakura texture (via mask-image), falling petals (sakura rain), falling shuriken, alert tile inset rings (all 9 alert styles via `color-mix(in oklch, var(--accent), transparent N%)`). Theme switch retints everything in real time.
- **Custom alert glyphs** (replaced lucide stock icons): pulse → halo ellipse, sakura → 3 scattered bezier petals, shuriken → 8-point star w/ punched center, ninja → balaclava silhouette w/ eye slits, andon trigger → paper lantern w/ flame.
- **Petal/shuriken physics unified**: rain particles use the bezier petal path from the static sakura texture w/ per-particle rotation+scale variance via `--p-rot`/`--p-scale`. Shuriken uses same fall+sway physics + slow inner SVG rotation.
- **Phase A persistence shipped** (`tauri-plugin-store` + `app/src/persistence.ts`): theme, accent, bg, texture, texAmt, fz, **tfz**, layout, alertStyle all survive relaunch via `settings.json` in app data dir. Validation enforces enum/range. Legacy `alertStyle` IDs (`tron`→`hotaru`, `katana`→`triple`) auto-migrate.
- **Alert-style system** (`document.documentElement.dataset.alertStyle` + `.tile.needs` CSS + per-tile `<AlertExtras>` DOM). 10 styles, all driven by `--accent`:
  - Andon (soft halo) · Kodou (lub-dub heartbeat) · Kokyu (slow breath) · Hotaru (racing firefly dot) · Samurai (katana sheen + 待 hanko) · Sandangiri (three slashes via clip-path) · Tatewari (vertical falling cut) · **Sakura rain** (renamed from "Sakura"; petals fall + pile up; uses bezier petal SVG) · Ninja (corner smoke + spinning shuriken) · Shuriken (8-throw + stick + smoke-fade)
  - All honor `prefers-reduced-motion`. Default = Andon.
- **PTY layer** (`app/src-tauri/src/pty.rs`): `spawn_session` / `send_input` / `resize_session` / `kill_session`. Inherits parent env. `[mc-pty]` eprintln diagnostics removed.
- **xterm.js wired** (`app/src/Terminal.tsx`): xterm 6 + fit-addon + WebglAddon. Per-theme palette swaps without re-spawning. Spawn-stability check waits for two equal cols/rows measurements before spawning the PTY (prevents off-by-one truncation).
- **Multi-session frontend hook** (`app/src/sessions.tsx`): SessionsProvider owns `Map<key, Session>` + lifecycle; 256 KB ring buffer per session for late-mount replay
- **Resize handles** (`app/src/GridResizeHandles.tsx`): track-level drag handles, rAF-coalesced, PTY IPC paused during drag and flushed once on release
- **Spawn / close flow**: `spawn()` opens native folder picker → derives session name from basename. Three entry points: titlebar `+`, ⌘N, empty-state CTA. App starts empty.
- **Confirm-close modal**: ⌘W and tile × open a modal; Enter confirms, Esc/click-outside dismisses
- **Default app size**: 1400×850, minWidth 900, minHeight 500, centered
- 47+ HTML mockups in `mockups/` (locked design baseline = `36-textures.html`; latest are `43-logo-marks.html`, `44-logo-marks-v2.html`, `46-textures-curated.html`, `47-navbar-buttons.html`)
- **PTY layer** (`app/src-tauri/src/pty.rs`): `spawn_session` / `send_input` / `resize_session` / `kill_session`. Inherits parent env. `[mc-pty]` eprintln diagnostics removed.
- **xterm.js wired** (`app/src/Terminal.tsx`): xterm 6 + fit-addon + WebglAddon. Per-theme palette swaps without re-spawning. Spawn-stability check waits for two equal cols/rows measurements before spawning the PTY (prevents off-by-one truncation). Claude's welcome-banner colors are driven by xterm's per-theme ANSI palette in `themeFor()` — the banner colors with the theme.
- **Multi-session frontend hook** (`app/src/sessions.tsx`): SessionsProvider owns `Map<key, Session>` + lifecycle; 256 KB ring buffer per session for late-mount replay
- **Resize handles** (`app/src/GridResizeHandles.tsx`): track-level drag handles, rAF-coalesced, PTY IPC paused during drag and flushed once on release
- **Spawn / close flow**: `spawn()` opens native folder picker → derives session name from basename. Three entry points: titlebar pill, ⌘N, empty-state CTA. App starts empty.
- **Confirm-close modal**: ⌘W and tile × open a modal; Enter confirms, Esc/click-outside dismisses
- **Reset sizes button** in titlebar — snaps tracks back to layout default, dispatches `en:refit`
- **Default app size**: 1400×850, minWidth 900, minHeight 500, centered
- **Phase A persistence shipped** (`tauri-plugin-store` + `app/src/persistence.ts`): theme, accent, bg, texture, texAmt, fz, layout, alertStyle all survive relaunch via `settings.json` in app data dir. Validation enforces enum/range. Legacy `alertStyle` IDs (`tron`→`hotaru`, `katana`→`triple`) auto-migrate.
- **Alert-style system** (`document.documentElement.dataset.alertStyle` + `.tile.needs` CSS + per-tile `<AlertExtras>` DOM). 10 styles, all driven by `--accent` so they auto-theme:
  - Andon (soft halo) · Kodou (lub-dub heartbeat) · Kokyu (slow breath) · Hotaru (racing firefly dot) · Samurai (katana sheen + 待 hanko) · Sandangiri (three slashes via clip-path, no final glow) · Tatewari (vertical falling cut from tile's top edge) · Sakura (petals fall + pile up to ~36, drifters dissipate at the floor) · Ninja (corner smoke + spinning shuriken) · Shuriken (8-throw + stick + smoke-fade, two waves)
  - All honor `prefers-reduced-motion`. Default = Andon.
  - Status detection isn't wired yet — `tile.status === "needs"` never fires from runtime today; preview via `pass-code.md` devtools snippet.
- **Window-drag artifact fixed**: switched `body::before` (texture) and `body::after` (vignette) from `position: fixed` → `position: absolute` so they don't get put on a separate compositor layer that lagged behind the window during drag on macOS WebView.
- **Project rename + git init** (2026-05-20): folder `multiclaude/`→`en/`, brand `multi·claude`→lowercase `en`, bundle `dev.sixicicle.en`, Cargo crate `en`/`en_lib`, refit event `multiclaude:refit`→`en:refit`. Memory dir moved to `~/.claude/projects/-Users-austin-pozodesportes-Documents-claude-projects-en/memory/`. Repo: `https://github.com/six-icicle/en` (HTTPS via gh token; SSH not authorized for this account). Initial commit pushed to `main`.
- **Status detection on `feat/status-detection`** (2026-05-20, **pushed but NOT MERGED**). Two commits ahead of main: `9921d0a feat: status detection via claude hooks → local HTTP → Tauri events` + `a7d6b8d chore(brand): set wordmark font to Outfit 600 @ 18px`. New `app/src-tauri/src/hooks.rs` runs a `tiny_http` receiver bound to 127.0.0.1:0 (dynamic port), listens for `POST /hook` with `{session_id, event}`, emits `tile-status:<id>` Tauri events. State machine: SessionStart→idle, UserPromptSubmit→working, Stop|Notification→needs, SubagentStop ignored. `pty.rs` injects `EN_HUB_SESSION_ID` and `EN_HUB_HOOK_URL` into every spawn. On startup the app idempotently merges a hook block into `~/.claude/settings.json` (with one-time `settings.json.en-backup-<ts>` backup). Smoke-tested live: tiles flip working↔needs on real claude prompts.
- **Brand font set to Outfit** (2026-05-20): wordmark is Outfit 600 / 16px / 0.04em letter-spacing, scales with `--fz`. Loaded alongside Inter + JetBrains Mono in `index.html`.
- **Aesthetic audit (2026-05-21)** — phases 5/6/1/2/3 swept on `App.css`:
  - Motion tokens: `--dur-fast: 120ms` + `--dur-slow: 250ms` at `:root`; ~22 transition substitutions; pulse animations unified at 1s.
  - Z-index documented in a comment block at top of CSS (decorative 9997–9999, chrome 50/100, tile internals 1–6) — no collisions found.
  - Spacing: padding/gap/margin in interactive chrome converted to `calc(Npx * var(--fz))` so titlebar/popover/tile-head/modal/foot/empty-state breathe with text scale. Borders, radii, blurs, fixed widths/heights left fixed (intentional).
  - Reduced-motion: added suppression for status-dot pulse + popover slide-in inside the existing `prefers-reduced-motion` block.
  - Focus-visible: single global rule (`button:focus-visible, [role="button"]:focus-visible, input:focus-visible`) → 2px accent outline + 2px offset. Closes the keyboard-nav hole that 14 of 16 interactive elements previously had.
- **Theme system overhaul (2026-05-21)** — 8 themes, 4 pairs:
  - Each family has a **default (high-contrast)** lead and a **-soft (lifted)** companion. Lifted recipe: bg pulled UP not down, ink tonally related to bg, accent restrained → "lit from within" instead of "crisp on void."
  - Families: **kanagawa**, **everforest**, **rose-pine**, **hinoki** (renamed from "Dark Amber" / "original" to fit the Japanese-natural-pigment family — warm-charcoal cypress + desaturated tea-amber accent, not OLED-black + neon).
  - **rose-pine-soft is the canonical reference + new app default** (`#2a273f` lifted-purple bg + lavender ink + restrained coral accent). Persistence migration handles legacy IDs (`original` → `hinoki`, `original-alt`/`original-soft` → `hinoki-soft`, `rose-pine-alt`/`rose-pine-alt-soft` → `rose-pine`/`rose-pine-soft`).
  - All 8 palettes wired in `App.css` (theme tokens + theme-picker dot gradients), `App.tsx` (THEMES list + ACCENT/BG maps), `Terminal.tsx` (xterm palettes), `persistence.ts` (Theme type + valid set + THEME_LEGACY migration map).
- **Body font swap** (2026-05-21): `--sans` Inter → **Space Grotesk** at body weight 500 (Inter at 400 read thin). Wordmark stays Outfit 600 (`--display`). Distinctive `a/g/t` terminals give the UI character without decorative chrome.
- **Navbar overhaul** (2026-05-21):
  - All chrome buttons (`+`, ↻, theme, layout, alert, app-fz, term-fz) are now uniform 30×30 frosted-glass icon-only pills. Removed THEME / LAYOUT / ALERT external word labels, trigger text, caret arrows.
  - Theme button = half-bg/half-accent circle (matches picker dots, retints with theme).
  - Alert button = dynamic `<AlertGlyph>` reflecting selected style (was hardcoded andon SVG).
  - Font-size pills: `Aa` (app) and box-icon glyph (terminal). Click → small popover with `−` / `100%` / `+` row. Both glyphs use `--accent`.
  - `gap: 8px` lifted to `.titlebar` itself (was redundantly stacked with per-button margin-lefts that drifted) → uniform spacing across all 7 nav buttons.
  - Empty-state CTA: `spawn` → `kindle one` (Japanese-natural verb fits the brand). `kbd` style now matches foot bar (transparent bg, ink-2 color). `⌘N` → `⌘ N` (space).
  - Reset (↻) button now opens a **ResetMenuModal** with 5 actions: terminal sizes, app font size, terminal font size, theme colors (drop accent + bg overrides), all appearance.
  - Titlebar + foot bar heights converted to `calc(Npx * var(--fz))` so they scale with app font size (no more clipping at fz ≥ 1.3).
- **Brand blueprint** (`mockups/49-brand-blueprint.html`) — single-file aesthetic spec rendered in en's actual style. 6 sections (essence, typography, color/themes, spacing/motion/elevation, components, voice) each with a copy-paste JSON block. Final mega-spec at the bottom is a one-payload paste-into-AI brief that includes an `ai_directive` summary line.
- **Audit skill saved** (`~/.claude/skills/audit-frontend/SKILL.md`) — designed but not built out yet. Three-phase pipeline: Discovery (6 read-only specialized agents in parallel) → QC (3 adversarial review agents: verifier/deduper/prioritizer) → Blast-radius trace (4 dependency-mapping agents on surviving findings). Produces a single merged ledger; user + Claude walk it together in Phase 3 (no fix agents — strict). Use `/audit-frontend` to invoke after a few more app changes settle.
- **User-level rule #5 added** to `~/.claude/CLAUDE.md`: *"Match the Family Before Adding to It"* — when introducing a new variant of an existing pattern, grep for sibling instances and copy the full contract (tokens, colors, defaults, error handling), not just the shape. Added after a navbar miss where new font-size triggers used `var(--ink-2)` while every other icon-only trigger used `var(--accent)`.
- **Foot bar + empty-state accent retint** (2026-05-21): `.foot`, `.foot kbd`, `.foot .right`, `.empty-state`, `.empty-state .empty-line`, `.empty-state .empty-cta` (text + kbd) all switched from `--ink-3`/`--ink-2` → `--accent`. Pill/kbd borders kept at `--line-2` (matches nav-button hairline shells). Theme-aware, retints on each theme switch. Pending user keep/revert eyeball but currently shipping.
- **Navbar bg → Sumi ink wash** (2026-05-21): replaced `color-mix(in oklch, var(--bg), black 35%)` (read too heavy — chrome sank below the lifted canvas) with `linear-gradient(180deg, color-mix(in oklch, var(--bg), var(--ink-4) 22%) 0%, var(--bg) 100%)`. Top edge holds weight via `--ink-4`, dissolves into canvas at the seam. Restrained, on-strategy with the lifted-bg recipe. Picked from a 13-variant collaboration in `mockups/50-navbar-bg.html` (7 self-designed + 2 each from `frontend-design`, `impeccable`, `ui-ux-pro-max` skills; color-coded skill chips). Recipe is from `impeccable`.
- **Custom macOS window controls** (2026-05-21): `decorations: false` in `tauri.conf.json` removes the entire native title bar package — title text, traffic lights, rounded corners, and standard window shadow are bundled in NSWindow's `.titled` styleMask flag and Apple makes them indivisible (no API to keep just the corners). Replaced with `<WindowControls />` React component: 3 themed 12px circle buttons in the titlebar leading slot (close / minimize / toggleFullscreen). Resting style is `--ink-3` at 30% alpha; hover fills with `--accent`. Drag region wired via `data-tauri-drag-region` attribute on `.titlebar` (Tauri 2's canonical attribute — `-webkit-app-region` is Electron-only and silently ignored). Capability permissions added to `app/src-tauri/capabilities/default.json`: `core:window:allow-start-dragging`, `core:window:allow-close`, `core:window:allow-minimize`, `core:window:allow-set-fullscreen`, `core:window:allow-is-fullscreen`.
- **Window now has square corners** (cost of `decorations: false`). Attempted CSS-radius restore via `transparent: true` + radius on `#root`/`.app` produced a white-corner artifact (WKWebView paints opaque white by default unless the OS layer also clears). Reverted. Real fix requires `macOSPrivateApi: true` — works but uses Apple private APIs (App-Store-incompatible; not a concern for en since it's a personal hub).
- **`.gitignore`**: added `.claude/worktrees/` so the in-flight `feat/status-detection` working copy can never accidentally be staged from the main tree.
- **Drag-drop files/folders/screenshots into tiles** (2026-05-21) — mirrors macOS Terminal.app. Drag onto a tile → POSIX-quoted path lands in that tile's prompt (no auto-Enter; multi-file drops join with spaces). Wired via Tauri 2's `getCurrentWebview().onDragDropEvent()` with StrictMode-safe async listener registration (cancelled-flag pattern fixed double-paste). Theme-aware **drop ring** rendered as a dedicated `<div className="tile-drop-ring">` (NOT a pseudo) to dodge cascade fights with `.tile.active::before/::after`; spans the full grid via `grid-row: 1 / -1`, sits at z-index 10 above xterm's opaque canvas. HTML5 `dragenter/dragover/drop` events suppressed at the xterm host (capture phase) so the textarea doesn't get a duplicate paste.
- **Foot bar accent retint** (2026-05-21) — `.foot` text + kbd glyphs read in `--accent`. Empty-state also retinted to `--accent` ("NO SESSIONS" + "kindle one ⌘ N" pill, including kbd) for theme coherence with the rest of the app.
- **Merged `feat/status-detection` → main** (2026-05-21). Resolved 3 conflicts: `app/index.html` (kept main's Space Grotesk + Outfit 600), `App.css .brand` block (kept main's torii-flex 16px / 0.04em), `Cargo.lock` (regenerated via `cargo check` for clean tiny_http resolution). Status detection now LIVE end-to-end: claude lifecycle hooks → curl → local tiny_http receiver (dynamic 127.0.0.1 port) → Tauri `tile-status:<id>` event → frontend session state → tile flips to `needs` class → selected alert animation fires.
- **Fixed `CLAUDE_CODE_SIMPLE=1`** in `pty.rs` — that env var was suppressing claude's welcome banner but ALSO disabled claude's hooks system entirely, so status detection silently never fired. Removed. Banner is back; hooks work. Net win.
- **Fixed `AlertExtras` grid bug** — the wrapper `<div class="alert-extras">` had no base CSS rule, so when `<AlertExtras>` rendered inside `.tile` (which is `display: grid` rows 38px / 1fr), the wrapper claimed a grid cell and squeezed `.t-body` to 0 height — terminal went blank during alerts. Added `.alert-extras { position: absolute; inset: 0; pointer-events: none; z-index: 4 }`. Same root pattern as the drop-ring grid issue earlier in the session: every direct child of a grid container claims a cell unless explicitly removed via `position: absolute`.
- **`ackStatus(key)` helper** on `useSessions` — clicking a tile that's in `needs` state flips it back to `idle` immediately so the alert animation stops on engagement instead of waiting for claude's next hook. The next real hook (`UserPromptSubmit` / `Stop`) overwrites as appropriate.
- **Live alert-style preview in picker** — hovering an alert option floats an animated 80×80 mini-tile to the LEFT of the row, rendering the actual `<AlertExtras>` against a `.tile.needs` stand-in inside `[data-alert-style="X"]` so per-style CSS fires normally. Single-instance via `alertHoverId` state, picker stays compact.
- **Alert glyph cleanup** — sandangiri (`triple`) trigger now renders three spaced diagonal slashes (was three stacked lucide `<Sword>` icons clipping the 30×30 pill due to a too-broad `.icon-only svg` width override). Tatewari (`vertical`) became a single diagonal slash matching sandangiri's middle slash so they read as siblings.
- **Removed `samurai` alert** (katana sheen + 待 hanko) and **`ninja` alert** (corner smoke + spinning shuriken). Surgical sweep: type unions, valid sets, picker entries, `<AlertExtras>` cases, `AlertGlyph` cases, all CSS rules + keyframes, z-index map refs, dead lucide imports. Legacy migrations `samurai → triple` and `ninja → shuriken` added to `persistence.ts` so existing users with those values saved get auto-bumped on next launch.
- **Latency investigation** — added wall-clock traces (Rust `eprintln`, JS `console.log`) timing the chain claude→curl→tiny_http→Tauri→React. Tried 500ms PTY-quiescence pre-fire to mask claude's slow `Notification` hook on picklists. Wrong call: it false-fired during mid-response pauses AND raced `ackStatus` (re-flipped to `needs` the moment claude redrew the cursor). Reverted. **Verdict**: pure responses fire instantly; only picklists lag ~5s. Generic time-based quiescence is the wrong tool. If revisited, the right approach is targeted picklist-pattern matching in PTY bytes (`❯`, `[1] ...`, `[y/N]`, etc.), not time-based detection.
- **Batch spawn `#` button** (2026-05-22) — new nav button, frosted `#` glyph, popover with kanji 一-八 buttons (Hiragino Mincho) + arabic numeral subtitles. Click `n` → native macOS folder picker opens with `multiple: true` → ⌘-click N folders → spawn one tile per pick. Picker title and `room` count slice the request to remaining capacity. Disabled options dim when count + n > MAX_TILES.
- **Kill-all (skull) button** (2026-05-22) — far-right of titlebar (trailing edge after term-fz pill). Resting glyph in `--ink-3` (destructive ≠ flashy), hover lifts to `--accent`. Disabled at 0 sessions. Click → `ConfirmKillAllModal` with dynamic count (`End N sessions?`); Enter to confirm wipes them all via `closeTile()` loop. ⌘W single-tile flow untouched.
- **Tile cap MAX_TILES=8** (2026-05-22) — `+`, ⌘N, `#` popover, and `spawn()`/`spawnBatch()` all respect it. `spawnBatch` clamps to remaining room; popover header shows live `n/8` counter. `tileCountRef` (synced from `tiles.length` via effect) lets the spawn callbacks read current count without stale-closure bugs.
- **Layouts simplified** (2026-05-22) — dropped `wide` (Hex 2×3) and `Quad` flavor. Three layouts: **Row**, **Grid** (2 rows × 1-4 cols, derived from count), **Focus** (extended to handle 7-8 with `2fr 1fr 1fr 1fr` × 2 rows). `LAYOUT_LEGACY` migration `wide → grid` in persistence.ts handles existing users. Dead `.layouts` CSS block (37 lines, sidebar picker from earlier UI iteration) deleted. Unused `Grid3x3` lucide import removed.
- **WebGL context cap** (2026-05-22) — module-level counter in `Terminal.tsx`, capped at 6 prod / 3 dev (`import.meta.env.DEV` switch). React StrictMode double-mounts effects in dev so disposed contexts aren't reaped before the second mount runs — without the dev halving, spawning 6 tiles raced past WebKit's ~8-context ceiling. Tiles past the cap silently fall back to xterm's default canvas renderer; visually identical, slightly slower on huge scrollback. Decrements on dispose so closing tiles frees slots.
- **`/audit-deadcode` skill** (2026-05-22, `~/.claude/skills/audit-deadcode/SKILL.md`) — sibling to `audit-frontend`, narrower + faster. 4 parallel read-only agents: `unused-exports`, `orphan-css`, `unreferenced-files`, `dead-types-and-legacy`. Each cites `file:line` and tags every finding `DELETE` / `INVESTIGATE` / `KEEP`. Output ledger merged into `findings.md`; user walks one-by-one, Claude makes deletions only on explicit per-item approval. Built after a `.layouts` zombie-CSS find motivated codifying the pattern.
- **Dead-code audit walked** (2026-05-22, ledger `.claude/audits/deadcode-2026-05-22-0827/`) — 9/9 DELETE actioned: 7 pre-xterm CSS spans (`pre.term`, `.p`, `.c`, `.t`, `.r`, `.k`, `.cur`, `.cur.dim`, `@keyframes cur`), `Status` export tightened to module-private (sessions.tsx), `TileStatusEvent` Rust struct + fields tightened to crate-private (hooks.rs), `GridResizeHandles` switched from local `Layout` redeclaration to importing canonical from persistence. 4 KEEPs verified load-bearing. Committed as `24ad67c chore: dead-code sweep`.
- **Frontend audit run** (2026-05-22, ledger `.claude/audits/2026-05-22-0851/`) — 6 discovery agents → 3 QC agents (verifier/deduper/prioritizer) → 4 blast-radius tracers. 139 raw findings → 5 critical, 21 warning, 95 info, 18 defer/rejected after rubric re-grade against en's project context (dark-only, single-user local, no compliance bar). Critical 5 cluster into 2 stories: (a) `hooks.rs` settings-file integrity (C2 substring-marker, C3 unbounded-append, C4 RMW race) + C5 corrupted-store overwrite, (b) C1 alert-preview HTML nesting (later demoted to defer — see C1 walkthrough below).
- **C1 walked + bug surfaced** (2026-05-22) — audit's C1 was JSX nesting (`<div>` inside `<button>`) in alert-style preview. First fix moved preview to popover-level sibling (one shared instance, key-remount on hover). Surfaced a *latent* cascade bug that always existed but was masked by per-hover remounts: `[data-alert-style="X"] .tile.needs` global rules cascaded through `<html>` ancestor into the preview tile, beating the preview wrapper's own attribute rule by source-order tie. **Final fix**: prefixed all global rules with `html` (only `<html>` matches now) + added `.alert-style-popover .alert-style-preview[data-alert-style="X"] .tile.needs` overrides at specificity 0,5,0 to beat `:not(.active)` outlier on the pulse rule (0,4,1). Preview now correctly shows hovered animation regardless of selection. JSX nesting reverted to original (preview inside `<button>`) — modern React doesn't warn with `aria-hidden="true"`, so C1's "broken behavior" claim was theoretical. Calibration note: audit tracer marked `net: contained` for C1 but the fix actually surfaced an adjacent CSS bug — recalibrate tracer confidence on similar findings.
- **All 5 audit Criticals shipped** (2026-05-22) — committed as 4 isolated commits so each is bisectable if regressions surface:
  - `230f4f0 fix(audit-c1)` — alert-preview cascade. Prefixed every global `[data-alert-style="X"]` rule with `html` so they only match the document root, plus `.alert-style-popover .alert-style-preview[...] .tile.needs` overrides at specificity (0,5,0) to beat the pulse rule's `:not(.active)` outlier (0,4,1). Original JSX shape preserved.
  - `23a1ced fix(audit-c2-c3)` — per-event upsert in `hooks.rs::install_hooks`. Walks `settings.hooks.<event>[*].hooks[*]`, replaces command of any entry whose command `starts_with(HOOK_MARKER)`, pushes a fresh entry if absent. Foreign entries untouched. Returns `Ok(false)` (no backup, no rewrite) when nothing changed. Drops the now-obsolete global `hooks_already_installed` short-circuit; the upsert is itself idempotent.
  - `f8ea0eb fix(audit-c4)` — advisory `fs2` exclusive lock on `~/.claude/.en-hooks.lock` guards the entire RMW cycle. Backup + tmp filenames now `{secs}-{nanos}-{pid}` so two same-second launches can't collide. New dep: `fs2 = "0.4"`.
  - `c569b85 fix(audit-c5)` — `appearance::quarantine_if_corrupt(app)` runs at boot in `setup()` *before* tauri-plugin-store opens the file. Reads `app_data_dir/settings.json`; if non-empty and unparseable as JSON, renames to `settings.json.en-corrupted-{stamp}` and logs to stderr. Plugin then opens fresh file, defaults load, original bytes survive for recovery. Plus `persistence.ts` loadAppearance now `console.warn`s the underlying error.
- **`feature-function-ideas.md` — all 6 items shipped** (2026-05-22). Walked one-at-a-time, ship → smoke → commit, each item bisectable:
  - `b1b10a4 feat(tile)` — Reveal-in-Finder icon next to ×. `<button class="t-reveal">` opens cwd via `openPath(s.cwd)` (not `revealItemInDir`, which opened the parent). Capability `opener:allow-open-path` needs an explicit `{ path: "**" }` scope — without it the command silently rejects (paths come from the user's own folder picker, so `**` is correct). Visible on tile hover/active/focus.
  - `01fa800 feat(tile)` — Close × retinted from `--warn` red to chrome family (`--ink-3` rest → `--accent` hover, no border, no bg pulse). ASCII `×` swapped for 12px SVG line-pair at stroke 1.75 to match the spawn `+` and reveal icons. `--warn` retained only for `.modal-confirm` (close + kill-all confirms) — the destructive intent now lives in the modal, not the icon. Same logic the kill-all skull already follows.
  - `ccad41b feat(terminal)` — Readline-style shortcuts via `xterm.attachCustomKeyEventHandler`. `ctrl+backspace` → `^W` (backward-kill-word), `cmd+backspace` → `^U` (unix-line-discard). `option+backspace` untouched (xterm's default `ESC+DEL` already maps to backward-kill-word, so option/ctrl converge — matches Terminal.app + iTerm2).
  - `480b9f1 feat(ux)` — `#` batch popover refactored from 6-col square-tile grid → vertical list of `[kanji] [N folders]` rows extending `.alert-style-option`. Inherits hover/active/disabled contracts from the alert popover for free. Net diff -36 lines. Dead `.batch-grid` + `.batch-num` rules deleted.
  - `4938456 feat(ux)` — Drag tile headers to **swap places**. Pointer-events (not HTML5 dragstart) + 5px activation threshold. Detection model: nearest non-source tile by center distance — works identically in Row, Grid, Focus (no per-layout branching, no slot-shift math, no asymmetry). Visual: dragged tile dims to 18%, non-target siblings dim via `filter: brightness(0.6)`, swap-target lights up with 1.5px accent inset ring + tinted bg + accent header gradient + name in `--accent`. FLIP-driven 520ms spring-overshoot animation on swap (`cubic-bezier(0.34, 1.15, 0.64, 1)` — Apple-style settle). Swap-animation options explored in `mockups/51-swap-animations.html` (4 looping side-by-side previews — spring won). Click on `.name` still focuses the tile; double-click enters rename mode (rename ergonomics preserved).
  - **Focus layout count=8 fix** (in same commit as Item 5) — `defaultTracks(layout, count)` had no count=8 case, falling through to count=7's `[2,1,1,1] × [1,1]` (8 cells, primary spans 2 = 6 small slots) which silently dropped the 8th tile. Now `[2,1,1] × [1,1,1,1]` = 12 cells, primary spans 4 rows × 1 col = 4 cells, 8 small slots for 7 sidebars + 1 unused. count=7 also tightened from `[2,1,1,1] × [1,1]` to `[2,1,1] × [1,1,1]` (9 cells exact, no waste). The CSS `.grid[data-layout="focus"][data-count]` rules are dead code — inline styles from `colFrs/rowFrs` always win. Confirmed but left in place; future cleanup.
  - `9b97b49 feat(ux)` — App brightness slider, new navbar pill (sun glyph) between term-fz and kill-all skull. Range 0.5–2.0 in 10% steps, popover with `[−] [%] [+]` mirroring the font-size pill family. State syncs to `--app-brightness` on `<html>`; `.app` reads it via `filter: brightness(var(--app-brightness, 1))` so brightness modulates every descendant — chrome, textures, xterm canvases — uniformly. Persists alongside other appearance settings; "reset all appearance" includes brightness.
- **Continuity sprint — Phase B-lite + Japan flag themes + polish** (2026-05-22, shipped as `a0e5fb9` + docs follow-ups `5da4730` / `b3d19da` / `9c06cc0`):
  - **Phase B-lite tile persistence** — `[{key, name, cwd, path}]` array saved to the same store as appearance via `loadTileSlots`/`saveTileSlots` (`persistence.ts`). On boot, restores tiles as `sleeping: true` stubs that render a `<SleepingTileBody>` ("click to revive" CTA + cwd path) instead of mounting xterm. Click revives → flips sleeping flag false → existing TerminalView mount path takes over (spawns fresh claude in cwd). Sleeping tiles dim to 78% brightness, name italic. Persistence trigger: any change to `tiles` array (renames, closes, reorders, spawns) re-serializes within the same effect that saves appearance.
  - **Washi Hinomaru theme** — en's first LIGHT theme. Cream washi bg `#f5efe4`, sumi ink, deep hinomaru red `#bc002d` accent. Picked by user from a 4-mockup spread (mockups/52–55: Claude / frontend-design / impeccable / ui-ux-pro-max all converged independently on "light Hinomaru" — strong signal). User then iterated on the palette via mockups/56-washi-palette-explainer.html (3-up rose-pine reference / before / after with plain-English labels for every token). Final palette uses cool-neutral light tones (`--bg #ebedf0`, `--tile #f1f3f6`, `--tile-2 #f8fafc`) — explicitly NOT warm cream per user direction. `--ok` is a confident "dodger" blue `#2a78c8` (HSL 212° 65% 47%); `--warn` is `#94004f` derived from accent via the same hue/sat/lightness shift rose-pine uses (accent → warn = -19° hue, +10pp sat, -8pp L). The lifted-bg recipe inverts: bg pulled DOWN from white into cream-neutral, `--tile-2` (lightest) becomes the most-elevated surface. Comprehensive light-theme override block scoped to `[data-theme="washi"]` retints every white-overlay rgba in the chrome (popover bgs, frosted pills, modals, foot/navbar gradients, tile head, grid gap dividers) to ink-toned alternatives that read on cream. xterm palette retuned for WCAG-readable ANSI on light bg — earth-tone shifted reds/greens/yellows; `white` is `#3a3026` (warm dark, claude's muted text); `brightWhite` is `#fbf6eb` (light, for inverted/highlighted contexts).
  - **Empirical finding — claude's picker bg is OUR `blue` token, not theme-independent**. User flagged "blue picker tile reads same color across all themes." Empirical test: set `washi.blue` to `#FF00FF` magenta, triggered a picker → tile turned magenta. Confirmed: claude emits ANSI 44 (`bgBlue`) for picker selection bg, mapped through xterm's `blue` slot, fully theme-controllable. Also verified `bgWhite` for highlighted-prompt blocks — tunable via `white` slot. Final blue: `#7291c0` ("20% lighter" per user feedback).
  - **Washi Kyokujitsu theme** — war-flag variant. Same shironuri palette as Washi Hinomaru, but renders the actual Wikimedia kyokujitsu SVG (Public Domain, 900×600, downloaded to `app/src/assets/kyokujitsu.svg` AND inlined as a `--kyokujitsu-svg` CSS data-URI custom property for instant render). Used in 3 places: (a) **app background** via `html[data-theme="washi-kyokujitsu"]::before` at z-index 0, opacity 0.32, `mix-blend-mode: multiply` (white field disappears, only red rays/disc come through as a watermark) — body bg made transparent for kyokujitsu so the layer shows through, `.app` sits at z-index 1 above; grid gap also transparentized to expose the flag between tiles; (b) **theme picker dot** — circular crop of the SVG instead of a CSS conic-gradient approximation; (c) **brand mark next to "en" wordmark** — 24×16 SVG (3:2 native aspect) replaces the disc-only mark for the kyokujitsu theme.
  - **Hinode (rising sun) alert style** — 9th alert in the family, idea borrowed from ui-ux-pro-max's mockup (#55) of "flag motif as a STATE not a logo." `<AlertExtras style="hinode">` renders a `.hinode-stage` with absolute-positioned disc + horizon hairline; disc rises from below the tile, peaks centered, settles partly above a glowing horizon line on a 3.2s loop. Specificity-prefixed under `html[data-alert-style="hinode"]` to fit the existing global cascade. Honors `prefers-reduced-motion`.
  - **Brand mark gets a hinomaru disc on washi themes** — replaces the torii SVG when `[data-theme="washi"]` or `[data-theme="washi-kyokujitsu"]`. JSX renders `<span className="brand-hinomaru">` after the "en" wordmark (per user "to the right of 'en'"); CSS hides the torii for those themes. Hinomaru = solid red filled circle; Kyokujitsu = the actual flag SVG at native aspect.
  - **Tile-head font now scales with `--tfz` (terminal pill), not `--fz` (app pill)** — 4 declarations flipped: `.t-head` font-size + gap + padding, `.t-head .name-edit` font, `.t-head .path` font, `.t-head .meta` font. Also added a `--tfz` setter useEffect on `<html>` so the new var actually exists at runtime (previously `tfz` only flowed into xterm's `fontScale` prop).
  - **Chrome non-selectable** — added `user-select: none` on `.titlebar`, `.foot`, `.alert-style-popover`, `.modal-overlay`, `.sleeping-revive`, `.sleeping-cwd`. Re-enabled selection inside `.xterm-host *` and `.t-head .name-edit`. User said dragging in chrome shouldn't accidentally highlight icons/labels.
  - **Drag-tile-swap polish (post-Item-5 followups)** — long iteration on the swap-target visual: started as a between-tile insertion bar that fought `:has()` cascade rules, refactored to "swap-on-drop" semantics (drop tile A onto tile B = trade slots; works identically in all 3 layouts since the model collapses to "nearest non-source tile by center distance"). Visual reworked to "non-target siblings dim via `filter: brightness(0.6)`, swap-target lights up with 1.5px accent inset ring + tinted bg + accent header gradient + name in `--accent`." FLIP animation: 520ms with overshoot tuned from 1.56 → 1.15 (subtler settle). Source tile dims to 18% during drag.
  - **Theme picker dot** — visual fight resolved: switched from `border: 1px solid` to `box-shadow: inset 0 0 0 1px` because semi-transparent borders on `border-radius: 50%` + `linear-gradient(135deg)` produced visible bleed at the gradient seam (cream-on-red half edge let red show through where the cream bg sat just inside the rounded clip — only visible on washi because cream-vs-red is the highest-contrast dot). Each dot's active state uses CSS custom properties `--dot-accent` and `--dot-glow` set per `[data-set]` so each dot's selected ring is in ITS OWN theme's accent (kanagawa amber, washi red, etc), not the currently-active theme's accent. Resting state matches the original (`--line-2` faint hairline) per user "I liked it the way it was originally" — not every-dot-in-own-accent.
  - **Mockups added** during this run: `52-japan-themes-claude.html` (3 directions: washi hinomaru / sumi kyokujitsu / asahi gradient), `53-japan-themes-frontend-design.html` (hinode), `54-japan-themes-impeccable.html` (shironuri), `55-japan-themes-ui-ux-pro-max.html` (Hinomaru + rising-disc alert idea), `56-washi-palette-explainer.html` (3-up palette comparison with plain-English token roles, used for the final palette tuning).
  - Shipped as a single feat commit (`a0e5fb9`) rather than the originally-planned 6-way split — coherent enough to bisect together. Subsequent docs commits log the work in `feature-function-ideas/` and archive the closed roadmap.
- **Washi Tsuki + war-flag easter egg + palette audit** (2026-05-23):
  - **Washi Tsuki** (`#11141a` cold-slate bg, `#c8cdd6` cool-grey ink, `#be4258` restrained shu accent) — dark/low-contrast sibling to the washi family. Picked from a 3-mockup arc: `57-washi-dark-explore.html` (4 candidates: sumi-yoru / akari / beni-yoru / yamizumi) → `58-yamizumi-cold.html` (3 coldness tunings) → `59-tsuki-red-tune.html` (3 red intensities; user picked variant A, `#be4258`). Wired across 7 sites: App.css palette + brand-mark hide + picker dot, App.tsx THEMES list + ACCENT/BG maps + brand JSX condition, persistence type/set, Terminal.tsx ANSI palette (cool-slate-tuned, sumi-on-slate not red-on-cream).
  - **Kyokujitsu cropped to circle** + **shows in empty-state bg** — the brand-mark next to "en" now uses `background-size: cover` to fit the 3:2 SVG into the 12px circle (central red disc reads at ~50%, rays radiate to edge, white field clips). The flag was already rendering full-bleed behind everything via `html::before`, but `.empty-state` was painting opaque `--tile` cream over it; making it transparent on `washi-kyokujitsu` exposes the watermark in the main window when no sessions exist.
  - **War-flag easter egg** — kyokujitsu hidden from picker; auto-applies when a tile is renamed to any of 15 case-insensitive trigger phrases: `war`, `wartime`, `at war`, `war mode`, `time for war`, `warpath`, `this means war`, `this is war`, `declare war`, `declaring war`, `i declare war`, `kyokujitsu`, `rising sun`, `asahi`, `hinode`. Contract is **option B**: theme can be switched away via picker, but title can stay; re-trigger requires renaming again (rename to anything else, then back, OR rename a different tile). Picker filter shows kyokujitsu only when it's the currently-active theme so the dot reflects reality + can be clicked off.
  - **Color audit** — ran WCAG contrast math on all 9 (non-tsuki) themes' bg-vs-ink/ink-2/accent/ok/warn ratios + accent-vs-warn separation. Caught 3 real outliers: (1) `kanagawa-soft` warn `#c34043` was failing the 3.0 UI floor at 2.78:1 → bumped to `#d44848`. (2) `everforest-soft` was the "washed-out" theme user flagged by feel — bg `#343f44` was at perceptual luminance ~12% (vs rose-pine-soft's ~6%, the user's gold standard). Pulled bg/tile/tile-2 down ~10 luminance points (`#2a3539`/`#303c40`/`#364046`), widened ink-2 (`#9da9a0` → `#8d9a90`) to sharpen the primary→secondary text gap, plus warn `#e67e80` → `#d4666e` for AA pass + less pinkish. Synced bg in 3 sites (App.tsx THEME_BGS, Terminal.tsx xterm bg + cursorAccent, App.css picker dot). (3) `washi` warn `#94004f` (magenta) → `#c44040` matching tsuki — user found the magenta too pink, fine to share warn across the washi family even though accent (`#bc002d`) and warn now both read as "red"; the warn is a destructive-confirm fill, not a frequent surface. Math/quirks documented in conversation: rose-pine soft + non-soft both have accent and warn in the same hue (intentional rose-family identity); everforest accent + ok are both green (sage-on-sage by design but worth knowing on a status-display app).
  - **Palette atlas mockup** (`mockups/60-palette-atlas.html`) — all-themes-all-tokens reference. Two sections: token reference legend (13 tokens × 2-4 plain-English bullets each, "what does --ink-3 actually paint?") + matrix table (10 themes × 13 tokens, hex below each swatch, transparency-bearing tokens on checkered backdrop, light theme marked ☀, default marked ★, tsuki tagged "new"). Built with help of two parallel Explore agents (one extracted JSON palettes, one mapped tokens to UI usage).
  - **Picker family separator** — washi pair (Hinomaru + Tsuki) now sits to the right of an extra-wide gap (`margin-left: 24px` on first washi dot vs standard `8px` inter-dot gap) — proximity grouping alone marks the boundary. First attempt added a hairline divider (`::before` pseudo at `-8px`), but the active-dot's accent glow ring on hinoki-soft made the line read visually off-center; killed the line, kept just the wider gap.
- **Audit walk + post-audit polish** (2026-05-25) — walked all 21 audit Warnings; 17 fixed across 7 bisectable commits, 4 deferred with rationale (W5/W9/W18/W20 — see `.claude/audits/2026-05-22-0851/walkthrough.md`).
  - **Cluster commits** (origin/main → HEAD): `09930d2` W1+W12 (extracted `usePopover` hook + modal Esc precedence via `stopImmediatePropagation`), `605bd99` W7+W8+W10+W14+W21 (small-CSS batch: bokeh theme-aware, --fz/--tfz scaling drift fixed, name-edit focus ring, dead `wide` CSS deleted, `[mc/]` log prefix renamed `[en/]`), `881bc49` W13+W15 (single-source catalogs in persistence.ts: THEME_META / TEXTURE_META / LAYOUT_META / ALERT_STYLE_META; Status→DisplayStatus exhaustiveness via Record), `d92d278` W6 (theme palette JS sources consolidated to new `themes.ts` — App.tsx + Terminal.tsx import THEME_BGS / THEME_ACCENTS), `6bb366d` W2+W3+W11+W16 (Petal hoist, FallingTexture both-kinds-mounted, ⌘W in rename input, Rust HOOKS table unified status_for + install loop), `682c758` W17+W19 (await store.save() flush + getStore() catch+null for self-heal), `3c3898b` W4 (SessionsContext split into Methods + List).
  - **Picker-dot chip artifact fix** (`6471484`): `.themes button` resting box-shadow swapped from opaque `var(--line-2)` to `color-mix(in oklch, var(--line-2), transparent 60%)` (hover 40%). Pre-existing AA artifact at the gradient seam on themes with opaque --line-2; only washi-tsuki was unaffected (its --line-2 was already rgba). Caught visually after audit cluster 4 made user scrutinize the picker.
  - **Drag-select bug** (`451318e`, bundled w/ post-audit polish): pre-existing — drag-swap pointer-down crossed into xterm content; `.xterm-host *` opts INTO `user-select: text` and beats body inline. Fix: handler toggles `body.dragging-tile` class on activation; CSS rule under that class sets `user-select: none !important` on every descendant including xterm. Restored on pointerup/cancel.
  - **Alerts on active tile suppressed** (same commit): pre-existing — only `pulse` had `:not(.active)` (with a separate `pulse-active` variant); the other 8 styles animated even when the tile was focused. All 8 now `.tile.needs:not(.active)`. `<AlertExtras>` JSX gated on `activeId !== s.key` so child elements (lasers, slashes, falling petals) don't render either. `pulse-active` rule + keyframe deleted. `ackStatus` removed from sessions.tsx (Methods + provider) — the CSS gate makes manual ack pointless; the underlying `Session.status === "needs"` is preserved so leaving the active tile re-exposes the alert (claude's "still waiting" signal stays accurate).
  - **Rename input pre-selects existing name** (same commit): `ref={(el) => el?.select()}` on the rename input. `autoFocus` alone left the caret at end; now typing replaces the selection.
  - **Light-theme texture support** (`d28e516`): pre-existing design gap — all 11 static textures used `mix-blend-mode: screen` with white-stroke SVGs (the standard dark-UI recipe). Screen-blending white-on-near-white is invisible on washi cream bg. Per-texture override block scoped to washi + washi-kyokujitsu: SVG strokes `%23ffffff` → `%230f0c0a` (washi sumi --ink), `mix-blend-mode: multiply`, opacity bumped 1.5× to compensate for Weber-Fechner perceptual asymmetry (humans perceive light-on-dark stronger than dark-on-light at same delta luminance, so multiply over light needs more α than screen over dark). 11 textures covered (grain, scanlines, dots, sakura, seigaiha, topography, pluses, yagasuri, brush, bamboo, bokeh). Future light themes inherit by adding their data-theme to each selector list.
  - **Walkthrough log** (`.claude/audits/2026-05-22-0851/walkthrough.md`, gitignored) — per-cluster smoke tests + revert paths + verified-deferral rationale for each unshipped warning. Reference for any future regression bisect.
  - **Memory written**: [[feedback-smoke-before-commit]] — before committing UI/CSS/event-handler changes, exercise the affected feature once in the running app. Typecheck + build alone are insufficient; today shipped 3 visible regressions/bugs (chip artifact, washi bokeh invisible, drag-select-in-xterm) that any 30-second smoke would have caught.

## What's next

### Long-term backlog (ordered by interest)

1. **(Parked, optional) Picklist pattern detection** — only worth attempting if the ~5s picklist alert lag becomes a real pain point. Approach: pattern-match recent PTY bytes for picklist markers (claude's `❯`, `[1]/[2]`, `[y/N]`, etc.) and pre-fire `needs` ahead of the slow `Notification` hook. Targeted not generic — time-based quiescence already proven wrong (false-fires mid-response, races `ackStatus`).
2. **Cross-theme contrast pass** (still deferred). Cycle through all 8 themes and eyeball legibility — especially accent-on-bg ratio for the new `hinoki` and `everforest-soft` palettes, and the new fully-accent foot bar + empty state. Easy to dial individual palette values if anything reads weak.
3. **Phase B persistence** — tile slots (cwd + name) restored on relaunch, manual click-to-spawn, missing-cwd shows a disabled state. Optional Phase C later: auto-respawn with `claude --resume <uuid>` from `~/.claude/projects/.../*.jsonl`.
4. **Layer 3 — distribution / enterprise readiness.** Apple Developer ID ($99/yr — user signing up now), code signing + notarization in `tauri.conf.json`, `tauri-plugin-updater` with minisign keypair, GitHub Actions to build signed `.dmg` on tag push.
5. Polish: find-in-scrollback (⌘F), drag-reorder tiles, SQLite metadata for titles/colors/recent cwds.
6. Consider Tauri `Channel<Vec<u8>>` instead of base64 events if streaming overhead shows up.

## Open decisions
- Categorical color palette for per-session tags (deferred until grid feels crowded)
- Whether sessions persist across app restarts or always start fresh
- Whether to wrap the `claude` CLI or invoke it directly
- Minimum tile width enforcement vs accepting narrow-tile clipping

## Key references
- **Always work from `/Users/austin.pozodesportes/Documents/claude-projects/en/`** — older sessions launched in the `…multiclaude/` cwd resolve memory to a stale path. Future Claude Code sessions should `cd en/` before starting.
- Memory directory: `~/.claude/projects/-Users-austin-pozodesportes-Documents-claude-projects-en/memory/`
- TauriForge persona + 5-section output protocol with `<thought>` tags: see memory `feedback-tauriforge-protocol.md`
- pass-code.md workflow: runnable code blocks go to `./pass-code.md`, prose stays in chat
- Mockups: `./mockups/` (locked design = `36-textures.html`; brand-font sampler = `42-brand-fonts.html`; navbar-bg study = `50-navbar-bg.html`; brand blueprint = `49-brand-blueprint.html`; swap-animation comparison = `51-swap-animations.html` — 4 looping options for tile-swap motion; **Japan theme exploration = `52-japan-themes-claude.html` / `53-frontend-design.html` / `54-impeccable.html` / `55-ui-ux-pro-max.html`** — 4 directions, user picked Washi Hinomaru; **palette explainer = `56-washi-palette-explainer.html`** — 3-up rose-pine vs washi-before vs washi-after with plain-English token roles, used for final palette tuning; in-flight palette exploration = `blues-greens-explore.html` — IBM Plex on near-black, blues+greens for hypothetical theme family)
- Project settings: `./.claude/settings.json` (allowlist), `./.claude/settings.local.json` (user-local, untouched)
- `~/.claude/settings.json` was modified by en's auto-merge (status-detection install). Backup at `~/.claude/settings.json.en-backup-<ts>`. The `# en-managed` marker in hook commands is what makes the merge idempotent.
